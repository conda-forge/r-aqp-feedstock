{% set version = "1.25" %}

package:
  name: r-aqp
  version: {{ version|replace("-", "_") }}

source:
  fn: aqp_{{ version }}.tar.gz
  url:
    - https://cran.r-project.org/src/contrib/aqp_{{ version }}.tar.gz
    - https://cran.r-project.org/src/contrib/Archive/aqp/aqp_{{ version }}.tar.gz
  sha256: f44d946b60e5568041b1854ca17b0de2e877fe244332d4e8a9448e2e0932c5ff

build:
  noarch: generic
  number: 0
  script: R CMD INSTALL --build .
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - r-base
    - r-hmisc
    - r-mass
    - r-rcolorbrewer
    - r-cluster
    - r-digest
    - r-lattice
    - r-plotrix
    - r-plyr
    - r-reshape
    - r-scales
    - r-sp
    - r-stringr
  run:
    - r-base
    - r-hmisc
    - r-mass
    - r-rcolorbrewer
    - r-cluster
    - r-digest
    - r-lattice
    - r-plotrix
    - r-plyr
    - r-reshape
    - r-scales
    - r-sp
    - r-stringr

test:
  commands:
    - $R -e "library('aqp')"  # [not win]
    - "\"%R%\" -e \"library('aqp')\""  # [win]

about:
  home: https://github.com/ncss-tech/aqp
  license: GPL (>= 3)
  license_file:
    - {{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3
  summary: A collection of algorithms related to modeling of soil resources, soil classification, soil profile aggregation, and visualization.
  license_family: GPL3

extra:
  recipe-maintainers:
    - ocefpaf
